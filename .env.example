# Public — safe to expose in the browser. Only lets the client read
# frases/autores and call the RPC functions in supabase/schema.sql.
VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=tu-anon-key

# SECRET — only used by scripts/migrate-frases.mjs on your machine.
# Never commit this, never import it into src/.
SUPABASE_SERVICE_ROLE_KEY=tu-service-role-key
